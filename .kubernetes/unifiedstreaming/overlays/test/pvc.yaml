---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-blob-unifiedstreaming-test
  namespace: customizable-test
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn-hetzner
