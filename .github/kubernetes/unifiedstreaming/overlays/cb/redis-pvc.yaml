apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-redis-data-cb
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-retain
  resources:
    requests:
      storage: 1Gi
