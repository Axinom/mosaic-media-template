apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-redis-data-test
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-hetzner
  resources:
    requests:
      storage: 5Gi
