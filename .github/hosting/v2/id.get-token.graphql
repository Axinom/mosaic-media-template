mutation GetManagedServiceAccountToken {
  authenticateManagedServiceAccount(
    input: {
      clientId: "#{managed-service-client-id}#"
      clientSecret: "#{managed-service-client-secret}#"
    }
  ) {
    accessToken
  }
}
