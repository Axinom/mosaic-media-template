mutation GetManagedServiceAccountToken {
  authenticateManagedServiceAccount(
    input: {
      clientId: "#{managedServiceClientId}#"
      clientSecret: "#{managedServiceClientSecret}#"
    }
  ) {
    accessToken
  }
}
